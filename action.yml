name: 'Conan action'
description: 'Run Conan tasks'
inputs:
  package:
    description: 'Package name and version ({name}/{version})'
    default:
  path:
    description: 'Package recipe folder path'
    default: .
  profile:
    description: 'Conan profile'
    default: default
  conan_repo:
    description: 'Conan repo name'
runs:
  using: 'node12'
  main: 'dist/index.js'
  post: dist/index.js
branding:
  icon: 'target'
  color: 'gray-dark'
